# S.tand 프로젝트 작업 규칙

이 파일은 S.tand 저장소의 개발, 테스트, 실기기 설치, Git 배포 및 TestFlight 배포 절차를 정하는 유일한 기준 문서입니다. 이전 문서나 관행이 이 파일과 충돌하면 이 파일의 최신 내용을 따릅니다.

## 규칙 문서 관리

- 프로젝트 규칙의 본문은 이 `PROJECT_RULES.md` 한 파일에서만 관리합니다.
- `AGENTS.md`는 자동화 도구가 이 파일을 찾도록 안내하는 진입점으로만 유지하며 규칙을 중복 기재하지 않습니다.
- 규칙을 변경할 때는 이 파일만 수정합니다.
## Codex 사용량과 크레딧 조회

- 사용량 절약과 남은 주간 사용량·크레딧 조회는 개인 공통 규칙의 `사용량 목표와 절약`, `CCMB 주간 사용량과 크레딧` 절차를 그대로 따릅니다. 다른 조회 방법이나 웹 검색을 만들지 않고 `/Users/armsone/.codex/bin/ccmb-usage`를 먼저 실행합니다.

## 개발 시작

- `STand.xcodeproj`는 프로젝트 설정을 Xcode UI에서 확인·변경하거나, 앱을 수동 실행·화면 검증해야 하는 등 실제로 필요할 때만 Xcode로 엽니다. 일반적인 소스 편집과 명령줄 빌드·테스트만으로 충분한 작업에는 Xcode UI를 열지 않습니다.
- 시뮬레이터는 기본 개발·검증 절차가 아닙니다. 화면 배치 확인, 시뮬레이터 전용 재현 또는 자동 테스트 실행에 실제로 필요한 경우에만 사용합니다.
- 시뮬레이터가 필요한 경우에는 `iPhone 17 Pro`를 첫 번째 확인 대상으로 사용합니다.
- 구현 후 관련 자동 테스트를 진행하되, 시뮬레이터가 필요하지 않은 테스트·빌드에 시뮬레이터를 임의로 실행하지 않습니다.
- 전체 테스트는 TestFlight·정식 배포 전, 여러 핵심 모듈을 동시에 변경한 경우, 공통 기반 코드·외부 의존성을 변경한 경우, 관련 테스트만으로 영향 범위를 판단하기 어려운 경우 또는 사용자가 요청한 경우에만 실행합니다.
- 반복 수정·설치 과정에서는 변경과 직접 관련된 테스트와 빌드 검증만 수행하고, 이미 통과한 전체 검증을 반복하지 않습니다.
- 마이크 입력, 박수 감지, 실제 화면 밝기처럼 시뮬레이터로 충분히 검증할 수 없는 항목은 실기기 단계에서 추가 확인합니다.
- 문서나 프로젝트 규칙만 변경하고 앱 바이너리에 영향이 없는 작업에는 Xcode와 시뮬레이터 실행을 요구하지 않습니다.

## 개발 속도·검증 효율·Mac 자원 보호

- 위험 기반 검증 순서, 재검증 회피, 시뮬레이터 병렬 제한과 복제 기기 정리, 디스크·자원 보호는 개인 공통 규칙의 `위험 기반 검증`과 `개발 자원과 플랫폼 안전`을 그대로 따릅니다. 이 프로젝트 고유 기준만 아래에 둡니다.
- 시뮬레이터가 필요하면 `iPhone 17 Pro`를 첫 번째 확인 대상으로 사용합니다.

## 실기기 설치 정책

- 일반적인 소스 수정은 로컬 변경과 위험도에 맞는 검증까지 수행합니다. `BK_iPhone17pro` 설치는 사용자가 이번 작업에서 설치를 명시적으로 요청한 경우에만 수행합니다.
- 설치 요청을 받으면 관련 검증을 완료하고 설치 후보에 필요한 빌드 번호를 한 번 반영한 뒤, 서명 빌드한 앱의 설치와 실행을 확인합니다.
- 실기기 설치 요청은 커밋·푸시·TestFlight 업로드를 포함하지 않습니다. 각각 사용자가 별도로 요청한 경우에만 수행합니다.
- 설치가 실패하면 성공으로 보고하지 않고 원인과 남은 검증만 알립니다. 사용자 소유의 관련 없는 변경은 수정하거나 커밋에 섞지 않습니다.

## TestFlight 업로드 정책

- TestFlight 업로드는 사용자가 이번 작업에서 명시적으로 요청한 경우에만 수행합니다. 날짜나 이전 관행만으로 업로드하지 않습니다.
- 업로드 전에 당일 이력이나 권한을 관행적으로 조회하지 않습니다. 실제 업로드에 필요한 최소 확인만 수행하고 업로드 도구의 직접 결과를 우선 근거로 사용합니다.
- 업로드 성공 여부를 추가 확인해야 할 때만 처리 상태를 제한적으로 한 번 조회합니다. 권한 오류가 확인되면 같은 권한 탐색과 실패한 업로드를 반복하지 않습니다.
- 실기기 설치, 커밋, 푸시와 TestFlight 업로드는 서로 별개이며 하나의 요청이 다른 작업을 자동으로 허용하지 않습니다.

## 고정 프로젝트 정보

- Xcode 프로젝트: `STand.xcodeproj`
- 스킴: `STand`
- 필요 시 확인 시뮬레이터: `iPhone 17 Pro`
- 실기기 이름: `BK_iPhone17pro`
- 번들 ID: `com.armsone.stand`
- 개발 팀: `T7B4EPLHPK`
- GitHub 원격: `https://github.com/armsone/S.tand.git`
- 기본 브랜치: `main`

## 제품 경험 기준

- 앱 아이콘을 새로 만들거나 수정할 때는 실제 에셋에 적용하기 전에 애플의 시스템 둥근 사각형 마스크와 중심 배치 가이드를 겹친 검토용 미리보기를 사용자에게 먼저 보여줍니다. 잠금화면 위젯용 단색 심벌은 앱 아이콘과 별도로 원형 규격에서 식별성을 확인합니다.

- 사용자에게 보이는 핵심 상태명은 `오브제 모드`, `매이트 모드`, `화들짝 모드`로 통일합니다. 과거의 `스탠드 모드`, `잠자기 모드`보다 이 명칭과 의미를 우선합니다.
- `오브제 모드`는 밝은 환경의 데스크 시계·날씨 오브제입니다. 수면 소리 감시와 자동 녹음을 하지 않고 후면 플래시도 켜지 않습니다.
- `매이트 모드`는 어두운 환경에서 최소 밝기의 시계·날씨를 남기고, 뒤척임을 살피며 코골이와 잠꼬대 후보만 녹음하는 상태입니다.
- `화들짝 모드`는 매이트 모드에서 뒤척임이나 움직임을 감지했을 때 화면과 보조 플래시가 잠깐 반응하는 일시 상태입니다. 지정 시간이 지나면 자연스럽게 매이트 모드로 돌아갑니다.
- 화들짝 모드의 후면 플래시는 최근 카메라 측정에서 방이 실제로 어둡다고 확인된 경우에만 켭니다. 방이 밝거나 측정값이 없거나 오래되었으면 화면만 반응합니다.
- 기본 전환은 `자동`이며 사용자는 `오브제 유지`와 `매이트 유지`를 즉시 선택할 수 있어야 합니다. 자동 상태에서 어두움이 파악되면 1분 안에 매이트 모드로 전환하고, 밝아질 때는 순간 조명과 실제 기상을 구분하도록 더 신중하게 확인합니다.
- 상하 드래그, 원터치 모드 전환, 고정 모드 및 뒤집어 놓기 동작은 앱 내부 조명만 조절합니다. 시스템 화면 밝기는 자동 모드의 참고값으로 읽을 수 있지만 앱에서 `UIScreen` 밝기를 쓰거나 변경하지 않습니다.
- 카메라 기반 주변 밝기 감지는 자동 매이트 모드에서만 작동합니다. 오브제 모드에서는 카메라 측정과 안내를 중단하고 아이폰 밝기를 읽어 앱 조명에 적용합니다.
- 카메라 밝기 확인은 사용자가 명시적으로 켠 경우에만 사용하는 보조 판단 수단입니다. 사진·영상 파일을 생성하거나 저장·전송하지 않고, 순간 영상 프레임의 평균 밝기만 기기 안에서 계산합니다.
- 카메라 밝기 감지 설명은 최초 카메라 권한 요청에서 한 번만 제공합니다. 이후 측정 때 앱 내부 반복 알림을 띄우지 않으며 iOS의 카메라 사용 표시는 숨기지 않습니다. 권한이 없거나 카메라를 사용할 수 없으면 해당 판단만 생략하고 시계·날씨·다른 센서 기능은 계속 작동해야 합니다.
- 기존 구현은 재사용 가능한 시계·날씨·녹음·편집 자산으로 보존하되, 사용자 흐름과 상태 충돌이 생기면 이 제품 경험 기준을 우선해 재구성합니다.

## 버전 정책

- 현재 공통 기준 `MARKETING_VERSION`은 `2.1.1`입니다. 중요 수정은 minor, 작은 수정은 patch를 올리고 major는 사용자의 직접 지시가 있을 때만 변경합니다.
- 현재 플랫폼 내부 `CURRENT_PROJECT_VERSION`은 `340977`, 사용자 표시 `BuildStamp`는 `202608251857`입니다.
- 설치·TestFlight·정식 배포에 사용할 새 후보를 만들 때만 플랫폼 내부 빌드와 `YYYYMMDDHHMM` 표시 빌드를 한 번 고정합니다. 단순 편집·컴파일·로컬 테스트마다 올리지 않습니다.
- 앱·Mac Catalyst·Widget·Share Extension의 제품 버전, 내부 빌드와 표시 빌드를 같은 후보 값으로 맞춥니다.
- 문서·규칙만 바뀌고 앱 바이너리가 변하지 않으면 빌드 번호를 올리지 않습니다.

## 빌드 이력

- `0.0.0`: 최초 앱 구현
- `0.0.1`: 완전 암전 시작, 터치 점등, 앱 전면 실행 중 자동 잠금 방지
- `0.1.0`: 세로·가로 자동 회전 및 방향 고정 설정
- `0.2.0`: 전체 수면 녹음 병합, 목록 재생·일시정지 및 하단 재생 진행바
- `0.3.0`: 플래시 동기 감광, 방향 고정 버튼, 플립시계 실루엣, 수면 소리 분류 및 배터리 보호
- `0.3.1`: 주요 버튼의 동작과 현재 상태 문구를 분리해 의미를 명확히 개선
- `0.3.2`: 상하 드래그로 화면 조명과 암전 시 정보 실루엣 밝기를 각각 조절
- `0.4.0`: 선택 녹음·오늘 녹음 병합 옵션과 첫 화면 하단 버튼 정렬 개선
- `0.5.0`: 핀치 시계 크기 조절, 시계 글꼴 선택, 내장 폰트 저작권·라이선스 제공 및 밝은 화면 시작
- `0.6.0`: 좌우 드래그 감광 대기 조절, 밝은 환경 감광 보류, 탭 감광, 실루엣 밝기 확장 및 현재 위치 날씨
- `0.7.0`: 다중 소리·동작 점등, 감광 중 탭 재점등, 잠금화면 실행 위젯, 녹음 재생·선택 개선, 초 표시 플립시계 및 날씨 확대
- `0.8.0`: 플립 테마 날씨·하단 버튼, 전체 플립시계 글꼴 미리보기, 날짜별 수면 타임라인, 0.5초 간격 선택 합본 및 내장 테스트 코골이 음원
- `0.9.0`: 자동 디밍·롱 터치 밝기 고정, 시계 중앙 정렬, 가로 날씨 재배치, 초·폰트 정렬, 투명 분할 간격, 재실행 초기화, 세로 감지 레벨·순간음 민감도 및 번인 방지 이동 개선
- `0.9.1`: 플립 패널 투명 간격 비율 조정 및 가로 날씨 아이콘·정보 분리 패널 적용
- `0.10.0`: 화면 편집 모드, 패널 배치·크기·결합, 12/24시간제, 환경 밝기 기준 전환 및 제스처 체계 개편
- `0.10.1`: 편집 배경 투명도, 날씨 패널 10% 겹침 단일 패널 결합·더블탭 분리, 체감온도 복원, 글꼴 3열 미리보기 및 패널 중심 자석 범위 개선
- `0.10.2`: 편집 모드 실제 화면 중복 제거, 패널 이동·확대 경계와 중앙 10% 자석, 전체 패널 그룹 핀치 확대, 가로 하단 여백 및 슬리핑·스탠드 좌우 표시 개선
- `0.10.3`: 편집 패널 하단 버튼·글꼴 팔레트 경계 보정, 안전 영역 기반 핀치 확대 제한, 숫자 글리프 중심·플립 간격 정렬, 슬리핑 배터리 패널 편집 및 잠자기 모드·밝기 명칭 적용
- `0.10.4`: 첫 화면 더블 탭 컬러·그레이스케일 테마 전환, 초 표시 명도 개선, 밝기 기준 플립 레일 및 앱 아이콘과 동일한 실행 위젯 적용
- `0.11.0`: 수면 소리 전체·오늘 선택과 선택 원본 합치기·합친 뒤 삭제·일괄 삭제 작업 추가
- `0.11.1`: 밝기 기준선이 현재 밝기 왼쪽이면 잠자기 감광, 오른쪽이면 스탠드 유지하도록 판정 방향 수정
- `0.11.2`: 선택 합본 제목을 선택 원본 중 첫 녹음의 시각으로 표시
- `0.12.0`: 하단 기능 버튼 소형화 및 밝기 기준 슬라이더를 동일한 플립 타일로 하단 버튼 사이에 통합
- `0.12.1`: AiShot·감지 종료 버튼 순서 교체 및 스탠드 모드 밝기 기준 타일 숨김
- `0.12.2`: 잠금화면 위젯 규격별 투명 템플릿 로고, 날씨 위치명·플립 중앙 정렬, 고정 위치 화살표 마키, 편집 상·하단 경계 가이드 및 밝기 기준 설정 타일 배치 개선
- `0.12.3`: 가로 편집 안전 영역·상하 경계선 재조정 및 밝기 기준 슬라이더 타일 투명도 통일
- `0.13.0`: 방향별 하단 버튼 순서 편집·자동 줄바꿈, 버튼 명도·글자 확대, 슬라이더 3칸, 전체 높이 편집 이동, 기본 서체·초 명도, 날씨 결합 기준 및 상태 문구 개선
- `0.14.0`: 편집 패널 위치 제한 제거, 가로·세로 중앙선 실시간 자석 정렬 및 시계 위치·크기 방향별 편집
- `0.14.1`: 하단 기능 버튼을 화면 폭에 맞춘 반응형 4열·8열 그리드로 확대하고 좌우·버튼 사이 여백 통일
- `0.14.2`: 밝기 기준 슬라이더 드래그 조절과 탭 잠자기·스탠드 근거리 1/4 기준점 전환 적용
- `0.14.3`: 편집 패널 왼쪽 위 조절점 드래그로 중심 고정 크기 변경 적용
- `0.14.4`: 편집 패널 핀치·왼쪽 위 조절점 크기 범위를 30~200%로 확장
- `0.15.0`: 홈 기능을 통합한 플립 테마 설정 제어판, 스탠드 모드 감광·감시 중지, 잠자기 모드 뒤척임 점등·코골이·잠꼬대 후보 녹음, 플래시 연동 기본값, 30분 재진입 기준 잠자리별 수면 타임라인 및 시뮬레이터 세로·가로 배치 기본값 적용
- `0.15.1`: 하단 안내 영역을 두 배 높이의 투명 버튼으로 확장해 숨겨진 기능 버튼을 직접 다시 여는 동작 추가
- `0.15.2`: 잠자기 모드의 뒤척임 소리·기기 움직임 점등 시 플래시 연동을 명시하고 일시적인 카메라 점유 후 재동기화 보강
- `0.15.3`: 플래시 연동을 신규 설치 기본값으로 유지하고 기존 설치도 업데이트 시 한 번 활성화한 뒤 사용자 해제 선택을 보존
- `0.15.4`: 감광 대기시간을 5초~5분으로 확장하고 이번 업데이트에서 5초를 한 번 기본 적용하며, 플래시를 연동 시 터치·뒤척임 100%, 해제 시 터치 0%·뒤척임 10%로 구분하고 하단 안내 위치 복원
- `0.15.5`: 시뮬레이터에서 조정한 세로·가로 패널 배치와 방향별 하단 버튼 배열을 신규 설치·초기화 기본값으로 반영하고 자동 돌봄 시작·종료 명칭을 명확히 개선
- `0.15.6`: 데스크 오브제 디자인을 앱 대표 아이콘으로 교체하고 동일한 단색 실루엣을 잠금화면 위젯에, 컬러 아이콘을 홈·설정 헤더에 적용
- `0.15.7`: 날짜 패널 말줄임 수정, 기기 뒤집힘 화면 암전·복귀, 앱 아이콘 확대 및 기존 오브제 기반 원형 잠금화면 심벌 적용
- `0.15.8`: 확정 앱 아이콘에 6% 여백과 하단 시각 중심 보정을 적용하고 앱 내부 브랜드 아이콘도 동일하게 조정
- `0.15.9`: 앱 아이콘을 이전 적용본보다 9% 더 축소하고 시각 중심을 화면 높이의 3% 아래로 보정
- `0.16.0`: 매이트 모드에서 1분간 주변 소리를 자동 학습하고 평균보다 커진 순간에 즉시 화들짝 반응하도록 단순화하며, 화들짝 지속 구간 타임라인과 수면 녹음 2배 재생 증폭을 추가
- `0.17.0`: Safari 공유 확장과 패널 편집에서 사용자가 합법적인 HTTPS 스트림 주소를 직접 가져와 자유 이동·크기 조절하는 포그라운드 인터넷 라디오 패널과 재생 중 소리 감지·녹음 상호배제를 추가하고, 홈의 AiShot·방향 고정 버튼을 제거
- `0.17.1`: iPhone·iPad 네이티브 지원을 추가하고 iPad 세로 화면의 하단 제어 버튼을 한 줄 반응형 그리드로 최적화
- `0.18.0`: 기기 밝기를 따르는 오브제 시작, 상하 반 화면 밝기 제어, 0·100% 모드 고정, 원터치 10·90% 매이트·오브제 전환, 불필요한 밝기 설정·하단 타일 제거 및 화들짝 모드 전용 플래시 적용
- `0.19.0`: 상하 1/4 화면 밝기 제어, 끝단 1초 유지 0·100% 고정, iOS형 밝기 현황판, 카메라 방 밝기 자동 전환, 30% 매이트 기준, 독립 초 패널, 4종 색상표 테마 및 자동 기능 명칭 개선
- `0.19.1`: 자동 기능이 꺼진 상태에서도 상하 밝기 조절이 작동하게 하고 드래그 중에는 기기 밝기만 갱신한 뒤 손을 뗄 때 한 번만 설정과 모드를 확정해 제스처 끊김을 수정
- `0.19.2`: 밝기·카메라 안내 레이어의 터치 충돌과 상단 겹침을 수정하고 상하 밝기 조절 거리를 화면 높이의 절반으로 통일
- `0.19.3`: 밝기 조절 중 화면 재구성을 일으키던 현황 패널과 관련 상태·타이머를 제거해 상하 드래그를 기기 밝기 직접 변경으로 단순화
- `0.19.4`: 상하 드래그를 시스템 밝기와 분리된 앱 조명 제어로 수정하고 전체 화면 제스처·간단한 앱 밝기 표시 및 설정 테마 색상표·더블 터치 안내를 개선
- `0.19.5`: 카메라 주변 밝기 감지를 자동 매이트 모드로 제한하고 오브제 모드에서는 아이폰 밝기를 앱 조명에 적용하며 반복 측정 안내를 제거
- `0.19.6`: 화들짝 모드의 후면 플래시를 최근 카메라 측정에서 방이 어두운 것으로 확인된 경우에만 켜도록 제한
- `0.20.0`: 다중 인터넷 라디오 채널 관리, 앱 내 주소 탐색 브라우저, 홈 라디오 롱터치 채널 편집 및 기존 단일 주소 자동 이전 추가
- `0.20.1`: 라디오 브라우저 기본 홈페이지·즐겨찾기 추가 및 스트리밍 주소 자동 감지·채널 자동 입력 제거
- `0.20.2`: 앱 밝기 조절값을 배경뿐 아니라 시계·날씨·라디오·하단 버튼을 포함한 화면 전체에 적용
- `0.20.3`: 과거 정상 동작하던 드래그 우선 배타적 제스처 구성을 복원해 화면 전체 상하 밝기 조절과 탭·롱터치의 입력 충돌 수정
- `0.20.4`: 홈 화면 최하단에 흐린 빌드 번호만 표시하고 하단 버튼 및 편집 경계가 겹치지 않도록 여백 반영
- `0.20.5`: 화면 전체 상하 밝기 드래그를 하위 패널보다 우선 처리하고 조절 중·고정 모드의 앱 밝기를 시스템 밝기 알림이 덮어쓰지 않도록 수정
- `0.20.6`: 라디오 재생 중 반복 안내와 불명확한 감지 레벨 표시를 제거하고, 상단은 왼쪽 현재 모드·가운데 S.tand 로고·오른쪽 배터리로 정렬
- `0.20.7`: 화면 중앙 현재 모드 패널을 제거하고, 시스템 밝기는 건드리지 않으면서 과거 정상 입력 구조의 전체 화면 동시 상하 드래그와 종료 시 밝기 저장 방식을 복원
- `0.20.8`: 탭·롱터치와 충돌하지 않는 iOS 전역 세로 팬 인식기로 앱 밝기 조절을 교체하고 빌드 번호 가독성을 개선
- `0.20.9`: 앱 밝기 전체 조절 거리를 화면 높이 전체로 늘려 중간값 선택을 부드럽게 하고 빌드 번호를 하단 보조 문구 크기로 확대
- `0.21.0`: 최신 기능은 유지하면서 검증된 0.18.0 방식의 단일 상하 드래그·시스템 화면 밝기 즉시 반영·앱 이탈 시 원래 밝기 복원을 적용하고 복잡한 앱 내부 감광 레이어와 끝단 타이머를 제거
- `0.22.0`: 자동 기능 종료 UI 제거, 매이트 40% 기준과 2초 원터치 전환, 인터넷 라디오 상태 문구·자동 재연결·홈 2채널 패널 및 40% 결합, 패널과 전체 화면 확대 경계 제한 제거
- `0.22.1`: 라디오 패널을 상태명만 보이도록 단순화하고, 밝기 100%를 1초 유지하면 오브제 모드를 잠그며 상단 잠금 상태와 하단 현재 밝기 표시 추가
- `0.22.2`: 오브제 모드 잠금 후 손을 떼는 미세 이동은 95~100% 완충 구간에서 흡수하고 의도적으로 95% 아래로 내릴 때만 잠금 해제
- `0.23.0`: 설정의 바로 제어 카드를 제거하고 인터넷 라디오의 재생·홈 패널 선택·추가·수정·삭제를 별도 관리 화면 없이 한 카드에 통합
- `0.23.1`: 인터넷 라디오 목록의 첫째·둘째 채널을 홈 패널로 자동 사용하고 숫자 선택을 제거하며, 펜슬을 누른 채널 바로 아래에 편집란 표시
- `0.23.2`: 화면 방향을 항상 자동 회전으로, 시간을 12시간제로 고정하고 화면·버튼 배치 설정과 버튼 순서 편집을 제거했으며, 권한·잠꼬대와 코골이·정보 중심으로 설정 화면을 단순화
- `0.23.3`: 인터넷 라디오를 두 채널로 제한하고 설정의 전체 구성 크기를 제거했으며, 권한·오브제 아이콘·설명 문구를 정리하고 테스트 아이폰의 세로·가로 배치를 기존 사용자에게도 한 번 기본 적용
- `0.24.0`: 시스템 밝기와 앱 조명을 분리하고 자동 모드 확인 지연·카메라 측정 주기·접근성을 개선했으며, 장시간 녹음·설정 복구·날씨·라디오 재연결·오디오 인터럽션·삭제 오류 처리와 Privacy Manifest를 보강
- `0.24.1`: 홈 화면 좌우 드래그로 인터넷 라디오 앱 내 볼륨을 조절하고 상하 밝기 제스처와 우세 축으로 분리
- `0.24.2`: 첫 권한 안내 뒤 카메라·마이크·위치를 순차 확인하고, 거부해도 허용 기능만으로 시작하며 누락 권한 안내를 3~7회 실행 간격으로 제한
- `0.24.3`: 라디오와 분리된 YouTube 영상·라이브·재생목록 패널과 공식 플레이어를 시험 추가했으나 영상별 외부 재생 제한으로 제거 결정
- `0.24.4`: YouTube 실험 기능을 제거하고 라디오 2채널 중심의 기존 화면과 설정으로 복원
- `0.29.5`: 잠소리 화면을 수면 리포트와 잠소리 관리로 구분하고, 시간별 잠자리 제목·밤사이 요약·공유·보관 현황 및 보이소 연동 검증을 반영
- `0.30.0`: iPhone·iPad 경험을 유지한 Mac Catalyst 지원, iOS↔Android 결정적 UI 카탈로그와 공개 홈페이지를 추가
- `0.30.1`: 설정의 음악 카드에서 Apple Music 최근 음악·Apple Music Classical·인터넷 라디오를 같은 채널로 전환하고, 홈의 두 음악 버튼에 원하는 채널을 각각 배치해 직접 재생하도록 적용. MusicKit 인앱 재생과 감지·녹음 상호배제, 키보드 위 소형 라디오 편집 버튼도 반영
- `0.30.2`: Apple Music·Apple Music Classical 홈 버튼이 음악 앱의 현재 곡을 먼저 이어 재생하고, 없으면 보관함 임의 곡, 보관함도 비었으면 Apple 추천 음악을 S.tand 안에서 재생하도록 우선순위를 보강하고 앱 표시·번들 이름을 `S.tand`로 통일. Mac에서는 시작 밝기를 100%로 고정하고 창 크기에 맞춰 배경 그라데이션을 확장
- `0.31.0`: 백그라운드 동작 기본값을 꺼짐으로 추가하고 권한·보이소·매이트 전환·배터리·잠소리 동작을 정비했으며, Mac에서 Apple Music의 현재 재생 곡과 상태를 복원하고 긴 곡명을 흐르게 표시하고 화면 편집의 인터넷 라디오 설정을 복구
- `0.31.1`: 홈 패널 크기와 좌우 시스템 볼륨 조절 동작을 정비
- `0.31.2`: 로고 아래 고정 음악 스트립, Apple Music·Classical과 인터넷 라디오 4칸, 설정 순서 편집, 재생 상태 분리와 다음 곡 동작을 적용
- `0.31.3`: 음악 제목 탭의 재생·다음 곡 동작을 구분하고 Mac 화면을 1.5배 확대했으며, iPad·Mac 설정 열 배치와 고정 라디오 4칸·화면 편집·잠소리 즉시 삭제 동작을 정비
- `0.31.4`: 잠소리 관리의 좌측 스와이프 즉시 삭제 입력을 보강하고 Mac의 시스템 음악 동기 조회로 인한 앱 멈춤을 앱 전용 MusicKit 재생으로 분리
- `0.31.5`: Mac 설정 화면 확대와 인터넷 라디오 바로가기·통합 편집 흐름을 정비하고 Catalyst 설치 후보를 갱신
- `0.31.6`: Mac 홈 음악 카드의 오른쪽 클릭 편집 모드, 드래그 순서 변경과 라디오 직접 수정 버튼을 추가하고 설정창 실제 크기 변경을 보완
- `0.31.7`: Mac 설정 시트의 초기 크기를 호스팅 컨트롤러 선호 크기로 적용하고 자유로운 창 크기 조절을 유지
- `0.31.8`: Mac 설정을 고정 크기 모달 시트에서 별도 창으로 전환해 1500×1000 기본 크기와 자유로운 창 크기 조절을 지원
- `0.31.9`: Catalyst 다중 창 제약으로 열리지 않던 설정을 현재 앱 창 전체 화면 설정으로 전환하고 잠소리·보이소 시트의 기존 닫기 동작을 복구
- `0.32.0`: Mac 잠소리·보이소 시트에 상위 화면이 직접 닫는 명시적 닫기 동작을 연결해 모달에서 빠져나오지 못하는 문제를 수정
- `0.32.1`: Mac 홈의 잠소리 확인·보이소를 설정과 같은 전체 화면으로 열고 닫기로 홈에 복귀하도록 전환
- `0.32.2`: 홈 하단 버튼 이름을 잠소리·보이소·설정으로 간결하게 통일하고 잠소리·보이소 화면의 완료 버튼을 설정과 같은 오른쪽 배치로 변경
- `0.32.3`: 아이폰 가로모드의 잠소리·보이소·설정을 음악 영역 오른쪽에 고정하고 남은 공간을 음악 패널에 할당했으며, 가로 기본 배치를 업데이트 시 한 번 재적용
- `0.32.4`: 아이폰 가로모드의 고정 제어 패널 폭과 편집 화면을 정리하고, 라디오 전환 시 재생 패널 자동 정렬 및 겹치지 않는 가로 기본 배치를 적용
- `0.32.5`: 대표 아이폰의 현재 가로 배치를 모든 Apple 기기의 공통 기본값으로 지정하고 업데이트 시 한 번 재적용
- `0.33.0`: 보이소에 주변 소리를 쓰지 않고 버튼으로 연결 화면을 부르는 무전기 역할·호출 반응을 추가
